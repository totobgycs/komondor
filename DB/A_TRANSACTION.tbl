CREATE TABLE A_TRANSACTION (
    TRANS_ID    D_IDENT NOT NULL /* D_IDENT = VARCHAR(10) NOT NULL */,
    TRANS_DATE  DATE,
    TRANS_DESC  D_DESCRIPTION /* D_DESCRIPTION = BLOB SUB_TYPE 1 SEGMENT SIZE 80 */,
    LASTMOD     TIMESTAMP
);
ALTER TABLE A_TRANSACTION ADD CONSTRAINT PK_A_TRANSACTION PRIMARY KEY (TRANS_ID);